language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t joydeepmazumder/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true joydeepmazumder/docker-react npm run test
